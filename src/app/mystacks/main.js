/*const scrollers = document.querySelectorAll(".stacks");

if (!window.matchMedia("(prefers-reduce-motion: reduce)").matches); {
    addAnimation();
}

function addAnimation(){
    scrollers.forEach(stacks => {
        stacks.setAttribute("data-animated", true)
    })
}*/

